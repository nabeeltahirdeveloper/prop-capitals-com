# Dependencies — will be installed fresh inside the image
node_modules

# Compiled output — will be rebuilt inside the image
dist

# Environment files — injected at runtime via docker-compose / docker run
.env
.env.*
.env.local
.env.*.local

# Test artefacts
coverage
.nyc_output

# IDE / OS noise
.vscode
.idea
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Git
.git
.gitignore

# Docker files themselves (not needed in image context)
Dockerfile
.dockerignore
